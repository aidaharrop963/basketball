var promptForPlayerName = function (playerNumber1) {
    var name = prompt(playerNumber1 + " what is your name?");
    return name; 
};
var tryTwoPointShote = function () {
    var Math.random()};
    if (Math.random() > 0.2);
    return true;
    else return false; 
};
